<!DOCTYPE html>
<html lang="en">

	<!-- head -->
	<%- include( '../partials/head.ejs', {page: 'Show'} )  %>

<body class="has-navbar-fixed-top">
  <!-- https://bulma.io/documentation/components/navbar/#fixed-navbar -->
  <%- include ( '../partials/nav.ejs') %>

  <section class="hero is-medium has-background-light">
    <div class="hero-body">
      <h1 class="title">Review Show Page</h1>
    </div>
  </section>

  <div class="container is-max-widescreen p-5">
    <article class="media">
      <div class="media-content">
        <div class="content">
          <h3>rating: <%= review.rating %>
          </h3>
          <small>Comment: <%= review.content %></small>
          <div class="field is-grouped mt-5">
		
            <div class="control">
              <a class="button is-info" href="/reviews/<%= review.id %>/edit">Edit Review
              </a>
            </div>

            <p class="control">
            <form action="/reviews/<%= review.id %>?_method=DELETE" method="POST">
                <input class="button is-danger" type="submit" value="	Delete Review"/>                    
            </form>
            </p>
          </div>
        </div>
      </div>
    </article>
  </div>
</body>

</html>